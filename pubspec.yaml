name: cambium_social_network
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: any
  flutter_riverpod: any
  freezed_annotation: any
  go_router: any
  flutter_sizer: ^1.0.5
  skeletonizer: ^1.1.1
  shimmer: ^3.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  freezed: any
  build_runner: any
  json_serializable: any


flutter:
  uses-material-design: true

  assets:
    - assets/